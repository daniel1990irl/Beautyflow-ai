# Beautyflow AI - Estructura base

## Requisitos
- Node 18+
- Cuenta Supabase (recomendada) o otra DB
- Cuenta Stripe (test keys)

## Instalación
1. Copia `.env.example` a `.env` y añade tus claves (SUPABASE_* y STRIPE_*).
2. Instala dependencias:
   ```bash
   npm install
